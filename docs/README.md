# Облачная платформа для управления автопарком

Проект представляет собой облачную платформу для управления автопарком с функцией мониторинга состояния транспортных средств в реальном времени.

## 🏗️ Архитектура проекта

Проект реализует микросервисную архитектуру на базе Docker Swarm с использованием следующих компонентов:

### Контейнеры (7 сервисов):

1. **PostgreSQL** - База данных для хранения информации о транспортных средствах
2. **Redis** - Распределенный кэш для повышения производительности
3. **API** (2 реплики) - Основной REST API сервис на FastAPI
4. **Dashboard** (1 реплика) - Веб-интерфейс для управления автопарком
5. **Nginx** - Reverse proxy и балансировщик нагрузки
6. **Prometheus** - Система мониторинга и сбора метрик
7. **Grafana** - Визуализация метрик и дашборды мониторинга

## 🚀 Быстрый старт

### Требования

- Docker Engine 20.10+
- Docker Swarm (опционально, для кластерного режима)
- Git

### Установка и запуск

#### Вариант 1: Docker Swarm (рекомендуется для продакшена)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd ProjectPlatform

# 2. Инициализируйте Swarm
docker swarm init

# 3. Соберите образы
docker build -t autopark-api ./src/api
docker build -t autopark-dashboard ./src/dashboard

# 4. Деплой стека
docker stack deploy -c docker-stack.yml autopark

# 5. Проверьте статус
docker service ls
```

#### Вариант 2: Docker Compose (для разработки)

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

#### Вариант 3: Использование скриптов (Windows)

```bash
# Запуск проекта
start-project.bat

# Остановка проекта
stop-project.bat
```

## 📡 API Endpoints

### Основной API (порт 8000)

- `GET /` - Информация о сервисе
- `GET /health` - Проверка здоровья сервиса
- `GET /vehicles` - Список всех транспортных средств
- `POST /vehicles` - Создание нового ТС
- `GET /metrics` - Метрики сервиса


## 🌐 Доступ к сервисам

После запуска проекта доступны следующие URL:

- **Dashboard**: http://localhost
- **API Documentation**: http://localhost/api/docs
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (логин: admin, пароль: admin)

## 🔧 Конфигурация

### Переменные окружения

Основные переменные можно настроить в `docker-stack.yml`:

- `DB_PASSWORD` - Пароль для PostgreSQL (по умолчанию: admin123)
- `GRAFANA_PASSWORD` - Пароль для Grafana (по умолчанию: admin)

### Сети

Проект использует overlay сеть `autopark-overlay` для межконтейнерного взаимодействия в Docker Swarm.

## 📊 Мониторинг

### Prometheus

Prometheus собирает метрики со всех сервисов:
- Метрики API сервиса
- Метрики телеметрии
- Системные метрики

### Grafana

Grafana предоставляет визуализацию метрик через интерактивные дашборды:
- Мониторинг производительности API
- Отслеживание состояния транспортных средств
- Использование ресурсов системы
- Алерты и уведомления

Подробная инструкция по настройке дашбордов: [monitoring/grafana/README.md](../monitoring/grafana/README.md)


## 🔄 CI/CD

Проект включает CI/CD pipeline на базе GitHub Actions (`.github/workflows/ci-cd.yml`):

1. **Build** - Сборка Docker образов
2. **Test** - Запуск тестов
3. **Push** - Публикация образов в Docker Hub
4. **Deploy** - Автоматический деплой в Docker Swarm

### Настройка CI/CD

1. Добавьте секреты в GitHub:
   - `DOCKER_USERNAME` - Имя пользователя Docker Hub
   - `DOCKER_PASSWORD` - Токен доступа Docker Hub

2. Pipeline автоматически запускается при push в `main` или `develop`

## 🏛️ Кластерная архитектура

Проект реализует кластерную архитектуру через:

1. **Docker Swarm** - Оркестрация контейнеров
   - 2 реплики API сервиса (кластер)
   - 1 реплика Dashboard
   - Автоматическая балансировка нагрузки через Nginx

2. **Kubernetes** - Альтернативная конфигурация (в папке `kubernetes/`)
   - Deployment с репликами
   - Service для балансировки
   - Ingress для маршрутизации

## 🔌 Сетевое взаимодействие

Все сервисы взаимодействуют через overlay сеть Docker Swarm:

```
┌─────────────┐
│   Nginx     │ (порт 80)
└──────┬──────┘
       │
   ┌───┴──────┬──────────┐
   │          │          │
┌──▼──┐  ┌───▼───┐  ┌───▼───┐
│ API │  │Dash   │  │Prometh│
│(2x) │  │(1x)   │  │eus    │
└──┬──┘  └───────┘  └───┬───┘
   │                    │
┌──▼──┐         ┌──────▼────┐
│Redis│         │Postgres   │
└─────┘         └───────────┘
```

## 📝 Структура проекта

```
ProjectPlatform/
├── .github/workflows/     # CI/CD конфигурации
├── database/              # SQL скрипты инициализации
├── kubernetes/            # K8s манифесты
├── monitoring/            # Prometheus конфигурации
├── nginx/                 # Nginx конфигурации
├── scripts/               # Скрипты деплоя
├── src/
│   ├── api/              # Основной API сервис
│   └── dashboard/        # Веб-интерфейс
├── docker-stack.yml      # Docker Swarm конфигурация
├── docker-compose.yml    # Docker Compose конфигурация
└── docs/                 # Документация
```

## 🧪 Тестирование

```bash
# Запуск тестов
docker-compose -f docker-compose.test.yml up --abort-on-container-exit
```

## 📈 Масштабирование

Для масштабирования сервисов в Docker Swarm:

```bash
# Увеличить количество реплик API
docker service scale autopark_api=5

# Проверить статус
docker service ls
```

## 🔒 Безопасность

- Используйте сильные пароли в продакшене
- Настройте TLS/SSL для Nginx
- Ограничьте доступ к портам Prometheus
- Используйте секреты Docker Swarm для чувствительных данных

## 📚 Дополнительная документация

- [Docker Swarm документация](https://docs.docker.com/engine/swarm/)
- [FastAPI документация](https://fastapi.tiangolo.com/)
- [Prometheus документация](https://prometheus.io/docs/)

## 👥 Авторы

Курсовой проект по настройке и администрированию облачной платформы

## 📄 Лицензия

MIT License

