version: '3.8'
services:
  test-api:
    build:
      context: ./src/api
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:///test.db
      REDIS_URL: redis://test-redis:6379/0
    command: ["uvicorn", "simple_main:app", "--host", "0.0.0.0", "--port", "8000"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 10s
    networks:
      - test-network
      
  test-redis:
    image: redis:alpine
    networks:
      - test-network

  test-runner:
    image: curlimages/curl:latest
    depends_on:
      test-api:
        condition: service_healthy
    command:
      - sh
      - -c
      - |
        echo '⏳ Waiting for API to be ready...'
        sleep 3
        echo ''
        echo '📊 Testing /health endpoint...'
        if curl -f -s http://test-api:8000/health > /dev/null; then
          echo '✅ /health returned 200 OK'
        else
          echo '❌ /health check failed'
          exit 1
        fi
        echo ''
        echo '📊 Testing / endpoint...'
        if curl -f -s http://test-api:8000/ > /dev/null; then
          echo '✅ / returned 200 OK'
        else
          echo '❌ / check failed'
          exit 1
        fi
        echo ''
        echo '📊 Testing /vehicles endpoint...'
        if curl -f -s http://test-api:8000/vehicles > /dev/null; then
          echo '✅ /vehicles returned 200 OK'
        else
          echo '⚠️  /vehicles check failed (acceptable for test without DB)'
        fi
        echo ''
        echo '📊 Testing /metrics endpoint...'
        if curl -f -s http://test-api:8000/metrics > /dev/null; then
          echo '✅ /metrics returned 200 OK'
        else
          echo '❌ /metrics check failed'
          exit 1
        fi
        echo ''
        echo '✅ All tests passed!'
    networks:
      - test-network

networks:
  test-network:
    driver: bridge